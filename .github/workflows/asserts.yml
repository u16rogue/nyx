# This workflow was generated by AI.
# It runs the repository's Nix assertions through the flake check entrypoint.
name: Asserts

"on":
  push:
  pull_request:

jobs:
  asserts:
    name: Run asserts
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v14

      - name: Run asserts
        run: |
          nix flake check --all-systems
          bash __tooling__/assert_state
          bash __tooling__/assert_templates
          bash __tooling__/assert_host_keys
